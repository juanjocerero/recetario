# Este fichero sirve como ejemplo y documentación de las variables de entorno necesarias.
# No copies los valores de aquí directamente a producción.

# Clave secreta para firmar los tokens de sesión (JWT).
# Debe ser una cadena larga, aleatoria y segura.
# Puedes generar una con: openssl rand -base64 32
SESSION_SECRET=""

# Contraseña del administrador hasheada con bcrypt.
# Se genera una sola vez y se guarda aquí.
# Puedes generar el hash con un script auxiliar.
ADMIN_PASSWORD=""


# Dirección de la base de datos PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/database"

# Variables de Autenticación (Better Auth)
# Genera un secreto seguro con: openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:5173"